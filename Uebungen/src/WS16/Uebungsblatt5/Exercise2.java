package WS16.Uebungsblatt5;

public class Exercise2 {
    public static void main(String[] args){
        double Bufsum= 1;
        double sum = 0;
        for (int k = 1; Bufsum > (1* (Math.pow(10,-5))); k++){
            Bufsum = 6 * (1.0 / (Math.pow(k,2.0)));
            System.out.println(sum += Bufsum);
        }
    }
}
